
const LineGradient = ({ width = "w-full" }) => {
    return <div 
            className={
                `h-0.5 ${width} bg-gradient-neon opacity-80 shadow-glow rounded-full`
            }
    >
        
    </div>
}

export default LineGradient;